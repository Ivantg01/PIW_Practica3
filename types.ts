
export type DataApi = {
    slug:string,
    company_name:string,
    tittle:string,
    url:string,
    jobs_types:string[],
    location:string,
    created_at:number
}

export type JobData ={
    id: number;
    title: string;
    description: string;
}
